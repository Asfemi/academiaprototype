import 'package:flutter/material.dart';

class SportsOtherInfo extends StatelessWidget {
  //const SportsOtherInfo({Key? key}) : super(key: key);

  static String id = 'other info for sports';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
