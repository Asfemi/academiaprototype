import 'package:flutter/material.dart';

class TeamsAndEvents extends StatelessWidget {
  //const TeamsAndEvents({Key? key}) : super(key: key);

  static String id = 'teams and events';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
